"use client";import Link from"next/link";import{useEffect,useState}from"react";import{Course,getCourses}from"@/lib/platform";export default function CourseCatalog(){const[items,setItems]=useState<Course[]>([]);const[error,setError]=useState("");useEffect(()=>{getCourses().then(r=>setItems(r.items)).catch(e=>setError(e.message))},[]);return <div className="course-grid">{error&&<div className="notice error">{error}</div>}{items.map(c=><Link href={`/courses/${c.slug}`} className="course-card" key={c.slug}><div className="course-level">{c.level}</div><h2>{c.title}</h2><p>{c.description}</p><div className="course-map">{c.lessons.map((l,i)=><span key={l.slug}><i>{i+1}</i>{l.title}</span>)}</div><b>{c.lessons.length} lessons · View course →</b></Link>)}</div>}
