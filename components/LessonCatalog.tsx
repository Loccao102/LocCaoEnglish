"use client";
import Link from "next/link";import{useEffect,useState}from"react";import{getLessons,Lesson}from"@/lib/api";
export default function LessonCatalog(){const[items,setItems]=useState<Lesson[]>([]);const[error,setError]=useState("");useEffect(()=>{getLessons().then(r=>setItems(r.items)).catch(e=>setError(e.message))},[]);return <div>{error&&<div className="notice error">{error}</div>}<div className="lesson-grid">{items.map(item=><Link href={`/learn/${item.slug}`} className="lesson-card" key={item.slug}><div className="lesson-meta"><span>{item.level}</span><span>{item.skill}</span><span>{item.estimatedMins} min</span></div><h2>{item.title}</h2><p>{item.description}</p><div className="lesson-footer"><span>{item.exercises.length} exercises</span><b>Start lesson →</b></div></Link>)}</div></div>}
