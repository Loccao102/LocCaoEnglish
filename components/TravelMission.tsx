"use client";
import Link from"next/link";import{FormEvent,useEffect,useMemo,useState}from"react";import{apiFetch,BossState,completeBoss,ConversationReply,getProgression,recordAttempt}from"@/lib/api";type Message={role:"agent"|"user";content:string};type Scenario="airport"|"hotel"|"transit";const configs={airport:{bossId:"airport-crisis",eyebrow:"TRAVEL · CHAPTER 1",title:"You missed your flight.",copy:"Build the three checkpoint skills, then negotiate a rebooking with the airline desk.",agent:"Airline service desk",intro:"Good afternoon. How can I help you with your flight today?",objective:"Explain that you missed your flight and ask to be rebooked.",reward:"Airport Wings"},hotel:{bossId:"hotel-reservation",eyebrow:"TRAVEL · CHAPTER 2",title:"Your reservation is missing.",copy:"Read the situation, repair polite requests, then solve the booking problem with the night receptionist.",agent:"Hotel night desk",intro:"Good evening. Welcome to the Meridian Hotel. How can I help you?",objective:"Explain that you have a confirmed reservation but the hotel cannot find it.",reward:"Hotel Diplomat title"},transit:{bossId:"city-transit",eyebrow:"TRAVEL · CHAPTER 3",title:"The last train is leaving.",copy:"Build transit language, catch platform information, then find a valid route across the city before service ends.",agent:"Metro information desk",intro:"Hello. Where are you trying to get to tonight?",objective:"Say where you need to go and ask for the fastest route before the last train.",reward:"City Navigator Frame + Passport Aura"}}as const;export default function TravelMission({scenario}:{scenario:Scenario}){const config=configs[scenario];const[boss,setBoss]=useState<BossState|null>(null);const[messages,setMessages]=useState<Message[]>([{role:"agent",content:config.intro}]);const[objective,setObjective]=useState(config.objective);const[text,setText]=useState("");const[turns,setTurns]=useState(0);const[busy,setBusy]=useState(false);const[claiming,setClaiming]=useState(false);const[notice,setNotice]=useState("");useEffect(()=>{getProgression().then(s=>setBoss(s.bosses.find(b=>b.id===config.bossId)??null)).catch(e=>setNotice(e instanceof Error?e.message:"Progression service is offline."))},[config.bossId]);const checkpoints=boss?.checkpoints??[];const unlocked=!!boss?.unlocked;const won=!!boss?.cleared;const progress=useMemo(()=>{const ready=checkpoints.filter(c=>c.complete).length;return checkpoints.length?Math.round((ready+(won?1:0))/(checkpoints.length+1)*100):0},[checkpoints,won]);async function chat(e:FormEvent){e.preventDefault();if(!text.trim()||busy||!unlocked||won)return;const userText=text.trim();setText("");setBusy(true);const next=[...messages,{role:"user" as const,content:userText}];setMessages(next);try{const result=await apiFetch<ConversationReply>("/v1/conversation/reply",{method:"POST",body:JSON.stringify({message:userText,scenario,level:"B1",history:next.map(m=>({role:m.role==="agent"?"assistant":"user",content:m.content}))})});setMessages([...next,{role:"agent",content:result.reply}]);setObjective(result.objective);const nextTurn=turns+1;setTurns(nextTurn);await recordAttempt({skill:"Speaking",activity:`npc-conversation-${scenario}`,itemKey:`${scenario}-npc-${nextTurn}`,prompt:objective,answer:userText,accuracy:.9});if(nextTurn>=3){setClaiming(true);const state=await completeBoss(config.bossId,92);setBoss(state.bosses.find(b=>b.id===config.bossId)??null);setNotice(`Chapter cleared. +${boss?.rewardXp??0} XP and ${config.reward} unlocked.`)}}catch(e){setNotice(e instanceof Error?e.message:"Coach unavailable")}finally{setBusy(false);setClaiming(false)}}return <div className="mission-shell"><section className="mission-header"><div><span className="eyebrow light">{config.eyebrow}</span><h1>{config.title}</h1><p>{config.copy}</p></div><div className="mission-progress"><strong>{progress}%</strong><small>chapter</small></div></section>{notice&&<div className="feedback-banner">{notice}</div>}<div className="mission-stage-grid">{checkpoints.map((stage,i)=><article className={`mission-stage ${stage.complete?"complete":""}`} key={stage.id}><span>0{i+1}</span><div><small>{stage.skill}</small><strong>{stage.title}</strong></div><div className="mission-stage-actions"><Link href={stage.route}>{stage.complete?"Practise again ↗":"Earn evidence ↗"}</Link><b>{stage.complete?"✓ READY":"NOT READY"}</b></div></article>)}</div><section className={`npc-panel ${!unlocked?"locked":""}`}><div className="npc-panel-head"><div><span className="npc-avatar">AI</span><div><small>FINAL CHECKPOINT</small><strong>{config.agent}</strong></div></div><span className="mode-badge">{won?"CHAPTER CLEARED":unlocked?"GATE OPEN":"GATE LOCKED"}</span></div>{!unlocked?<div className="npc-lock"><strong>{scenario==="airport"?"Build proof in all three skills.":"Clear the previous chapter, then create fresh evidence for these checkpoints."}</strong><p>Only successful attempts recorded by the backend count. Evidence for later chapters must be earned after the previous boss clear.</p></div>:<><div className="objective-box"><small>CURRENT OBJECTIVE</small><strong>{won?"Success — this chapter is complete.":objective}</strong></div><div className="chat-log">{messages.map((m,i)=><div className={`chat-bubble ${m.role}`} key={i}><span>{m.role==="agent"?"NPC":"You"}</span><p>{m.content}</p></div>)}</div>{!won?<form className="chat-form" onSubmit={chat}><input value={text} onChange={e=>setText(e.target.value)} placeholder="Type what you would say in English…"/><button className="button primary" disabled={busy||claiming}>{claiming?"Claiming reward…":busy?"Thinking…":"Send →"}</button></form>:<div className="boss-win"><span>✦</span><div><strong>Chapter cleared +{boss?.rewardXp??0} XP</strong><p>{config.reward} is now available. Continue the Travel District campaign.</p></div><Link href="/travel" className="button primary">Continue campaign →</Link></div>}</>}</section></div>}
