import{SentenceBuilder}from"@/components/MiniGames";export default async function Page({searchParams}:{searchParams:Promise<{pack?:string}>}){const{pack}=await searchParams;return <div className="page-wrap narrow-page"><header className="simple-header"><span className="eyebrow">GRAMMAR GAME</span><h1>Sentence Builder</h1><p>Build the sentence patterns needed by your current campaign.</p></header><SentenceBuilder pack={pack}/></div>}
