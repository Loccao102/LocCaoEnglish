import LessonCatalog from "@/components/LessonCatalog";export default function LearnPage(){return <div className="page-wrap"><header className="content-header"><div><span className="eyebrow">LEARNING LIBRARY</span><h1>Structured lessons, powered by the same adaptive engine.</h1><p>Lessons come from the content service, not hard-coded pages. Every checked answer emits the same learning events used by games and review.</p></div><span className="mode-badge">PostgreSQL CMS</span></header><LessonCatalog/></div>}
